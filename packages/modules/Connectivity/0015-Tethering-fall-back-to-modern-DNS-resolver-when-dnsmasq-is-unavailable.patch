From 89b06f0f98953307be39da08bfbcef1dd207f019 Mon Sep 17 00:00:00 2001
From: OpenCode <opencode@example.com>
Date: Wed, 29 Apr 2026 15:00:44 +0700
Subject: [PATCH] Tethering: fall back to modern DNS resolver when dnsmasq is
 unavailable

---
 .../src/com/android/networkstack/tethering/Tethering.java   | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/Tethering/src/com/android/networkstack/tethering/Tethering.java b/Tethering/src/com/android/networkstack/tethering/Tethering.java
index 722e81c2c4..88ca65816c 100644
--- a/Tethering/src/com/android/networkstack/tethering/Tethering.java
+++ b/Tethering/src/com/android/networkstack/tethering/Tethering.java
@@ -2095,7 +2095,11 @@ public class Tethering {
                 try {
                     // Stop and retry.
                     mNetd.tetherStop();
-                    NetdUtils.tetherStart(mNetd, true /** usingLegacyDnsProxy */, dhcpRanges);
+                    // dnsmasq may have failed on BPF-less kernels. Retry
+                    // without the legacy DNS proxy so NetworkStackService
+                    // provides its own DNS resolver instead.
+                    mLog.log("Legacy DNS proxy (dnsmasq) start failed, retrying with modern DNS resolver");
+                    NetdUtils.tetherStart(mNetd, false /** usingLegacyDnsProxy */, dhcpRanges);
                 } catch (RemoteException | ServiceSpecificException ee) {
                     mLog.e(ee);
                     transitionTo(mStartTetheringErrorState);
-- 
2.54.0

